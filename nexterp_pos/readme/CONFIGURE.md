Turn on **Multiple Open Sessions** on the register, under *Point of Sale >
Configuration > Settings*, in the **Orders** section. Every register decides
for itself; the rest of the database is untouched.
