This module adds a **Journal** field on analytic distribution models.

A model pinned to a journal is only applied to journal items posted in that
journal; items from any other journal ignore it. Models left without a journal
keep applying everywhere, as before.
